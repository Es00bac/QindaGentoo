#!/bin/bash
# Apply the committed delivery on the laptop, using the existing binhost.
# Commit/publish the overlay and stage its exact archives on qinda first.
set -euo pipefail
LAPTOP="${LAPTOP:-qinda-top}"
exec ssh "$LAPTOP" /usr/local/bin/qinda-sync
